pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "BlueKeyWorkshop"
include ':app'

// Resolve MeowUI from the checked-in source so UI and preference APIs stay in sync.
includeBuild('MeowUI')
